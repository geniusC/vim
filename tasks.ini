[cmake_build_debug]
command=[ ! -d build ] && mkdir build || cd build && cmake ../ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_EXPORT_COMPILE_COMMANDS=1 && pwd && cp compile_commands.json ../ && make -j16
cwd=<root>

[make]
command=pwd && cd build && pwd &&  make -j16
cwd=<root>
